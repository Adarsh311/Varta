package com.example.model

enum class ReadingDensity(val label: String) {
    MAGAZINE("Magazine"),
    COMPACT("Compact")
}

enum class NavigationTab(val label: String) {
    FEED("Discourse"),
    SEARCH("Search"),
    SAVED("Saved")
}
